import React from 'react';
import { FormattedMessage } from 'react-intl';

const Home = () => (
    <React.Fragment>
        <FormattedMessage id={'home.content'} />
    </React.Fragment>
);

export default Home;
